# Pinned to an exact, tested-together set for reproducible installs across machines.
# Verified: full dashboard (all tabs incl. Wages), timezone helpers, and theme proxy
# build cleanly against these versions on Python 3.13.
dash==4.2.0
plotly==6.8.0
pandas==3.0.3
numpy==2.4.6
gspread==6.2.1
google-auth==2.53.0
google-auth-oauthlib==1.4.0
Flask==3.1.3
Werkzeug==3.1.8
# Required for zoneinfo on Windows (no system tz database). Linux hosts already
# ship one; the app also falls back to a fixed UTC+10 if this is missing.
tzdata==2025.1
# Only used by the Render/gunicorn deployment; harmless elsewhere.
gunicorn==23.0.0
